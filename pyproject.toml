[project]
name = "pigia-shuru"
version = "0.1.0"
description = "Voice-first tax assistance backend built with FastAPI, Twilio, LiveKit, and Gemini Realtime."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
